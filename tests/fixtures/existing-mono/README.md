# Seller Desk

Seller Desk is an internal operations product. The current release lets operators search sellers and review account status.

## Development

- Web: `npm run test:web`
- API: `go test ./...` from `services/api`

The current production version is `v1.4.0`.
